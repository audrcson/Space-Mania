package mainWindow;

import javax.swing.*;

public class MainFrame extends JFrame {
    public MainFrame(){
        init();
    }

    public void init(){
        setVisible(true);
        setResizable(false);
        setTitle("SPACE SHOOTER GAME");
        setLocationRelativeTo(null);

    }
}
